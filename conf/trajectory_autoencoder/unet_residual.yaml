# @package _global_
defaults:
  - config

architecture: "unet_residual"
latent_dim: 128
output_dir: "logs/trajectory_autoencoder/unet_residual"

# U-Net Residual specific parameters
model:
  encoder_channels: [64, 128, 256]
  decoder_channels: [256, 128, 64]
  use_batch_norm: true

